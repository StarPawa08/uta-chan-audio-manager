<Window x:Class="UtaChanManager.MainWindow"
        Style="{StaticResource MaterialDesignWindow}"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:UtaChanManager"
        xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
        xmlns:models="clr-namespace:UtaChanManager.Models"
        xmlns:viewModels="clr-namespace:UtaChanManager.ViewModels"
        d:DataContext="{d:DesignInstance Type=viewModels:WindowSelectionVm, IsDesignTimeCreatable=False}"
        mc:Ignorable="d"
        Title="Uta-chan Audio Manager" Height="800" Width="600">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        
        <Button Content="Actualizar Lista"
                Click="Refresh_Click"
                Margin="10"/>

        <ItemsControl Grid.Row="1" ItemsSource="{Binding (viewModels:WindowSelectionVm.Windows)}" Margin="10">
            <ItemsControl.ItemTemplate>
                <DataTemplate>
                    <DataTemplate.DataType>
                        <x:Type TypeName="models:WindowInfo"/>
                    </DataTemplate.DataType>
                    
                    <StackPanel Orientation="Horizontal" Margin="0,5">
                        <Image Width="16" Height="16"
                               Margin="0,0,8,0"
                               Source="{Binding Icon}">
                        </Image>
                        <CheckBox Content="{Binding Title}"
                                  IsChecked="{Binding IsSelected}">
                            <CheckBox.Style>
                                <Style TargetType="CheckBox" BasedOn="{StaticResource MaterialDesignCheckBox}">
                                    <Style.Triggers>
                                        <DataTrigger Binding="{Binding IsActive}" Value="True">
                                            <Setter Property="FontWeight" Value="Bold"/>
                                            <Setter Property="Foreground" Value="MediumPurple"/>
                                        </DataTrigger>
                                    </Style.Triggers>
                                </Style>
                            </CheckBox.Style>
                        </CheckBox>
                    </StackPanel>
                </DataTemplate>
            </ItemsControl.ItemTemplate>
        </ItemsControl>

    </Grid>
</Window>
